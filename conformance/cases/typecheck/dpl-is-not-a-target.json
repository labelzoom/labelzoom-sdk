{
  "id": "typecheck/dpl-is-not-a-target",
  "note": "1.1: DPL is source-only. In a statically typed language there is no TargetFormat.DPL to name, so this must not compile.",
  "given": {
    "snippet": "client.convert().fromZpl(body).to(DPL)"
  },
  "expect": {
    "compileError": true
  }
}
